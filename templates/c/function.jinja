{{RETURN_TYPE}} {{FUN_NAME}}({% for p in PARAMETERS %}{{p.type}} {{p.name}}{{ ", " if not loop.last else "" }}{% endfor %}) {
{% if BODY %}  {{BODY | indent(2)}}
{% endif %}  return {{RETURN_VALUE}};
}